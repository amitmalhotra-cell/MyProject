pipeline {
    agent { docker { image 'sitespeedio/sitespeed.io' } }
    stages {
        stage('build') {
            steps {
                sh "echo 'shell scripts to run unit tests...'"
            }
        }
    }
}